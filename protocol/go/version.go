// Package protocol is the Go binding for the Layup wire contract shared by the
// Electron desktop and the Go control service.
package protocol

// Version is the protocol version spoken by this build. It must match
// protocol/VERSION and the TypeScript binding in protocol/ts.
const Version = 1
