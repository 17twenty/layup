/**
 * TypeScript binding for the Layup wire contract.
 *
 * Must stay in lockstep with protocol/VERSION and the Go binding in protocol/go.
 */

/** Protocol version spoken by this build. */
export const PROTOCOL_VERSION = 1;
